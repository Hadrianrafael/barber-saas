# Roadmap — V1 comercial

Vertical slices. Each slice ships a flow that works end to end (UI → server →
DB → side effects) and is tested before the next begins. Order follows the
priority list in the project brief: get sellable flows in first.

| # | Slice | Scope | State |
|---|---|---|---|
| 0 | **Foundation** | Repo, tooling, CI, Docker, Next app skeleton, full Prisma multi-tenant schema, env validation, i18n (pt-BR/en/es), Azure IaC, docs/ADRs | ✅ done |
| 1 | **Auth · Multi-tenancy · RBAC** | Sign up, sign in/out, e-mail verification, password reset/change, opaque sessions, tenant isolation layer (`forTenant`), role guards (`requireTenantContext`), separate Super Admin realm + console shell | ✅ core done |
| 2 | **Tenant onboarding + settings** | 3-step create-barbershop wizard (identity → contact/public → hours) with backend slug validation + stable `/barber/{slug}` URL; settings area (profile, branding upload, region, hours, holidays, booking config) RBAC-gated; per-tenant country/currency/timezone/locale (`src/lib/regions.ts`, ADR 0004); post-onboarding plan step → Stripe Checkout when configured, trial otherwise (**paid activation only via webhook — Slice 5**); public page scaffold (booking flow → Slice 9) | ✅ done |
| 3 | **Team · Services · Schedule** | Barber CRUD (per-barber work hours + daily break, time off / vacation, commission % or fixed, status — deactivation preserves history); service CRUD with **price/duration/buffer snapshot onto the Appointment**; agenda day / week / month with barber + status filters; full lifecycle PENDING→CONFIRMED→IN_PROGRESS→COMPLETED plus CANCELED / NO_SHOW; reusable scheduling domain (`getAvailableSlots`, `createAppointment`, `reschedule/cancel/confirm/start/complete/noShow`) shared by dashboard, public page (Slice 9) and chatbot (Slice 10); **conflict prevention** = availability check + SERIALIZABLE tx re-check + Postgres `btree_gist` exclusion constraint (concurrency-tested); all instants stored `timestamptz`, rendered in the tenant timezone; owner dashboard adds today / upcoming / working-today / open-slots / cancellations / no-shows. ADR 0005. | ✅ done |
| 4 | **Clients CRM** | Customer CRUD + dup detection, segmentation (reusable `where` builder), search/filter/pagination, appointment history, per-channel consent (opt-in/opt-out), GDPR erase, metrics, completed-appointment stat rollup. ADR — | ✅ done |
| 5 | **Stripe subscription (SaaS billing)** | Plans as data (price + `limits` JSON, monthly/yearly), `/pricing` + `/billing`, Stripe Checkout, idempotent webhook `POST /api/webhooks/stripe` (`WebhookEvent` de-dup), full lifecycle (trial → active → past_due+grace → canceled) synced from `customer.subscription.*` / `invoice.*`, `Payment`+`Invoice` ledger, customer portal, server-side plan gating (`assertWithinLimit`/`assertFeature` on employees/services/customers), env-gated — no simulated payments. ADR 0006, docs/deployment/stripe.md | ✅ done |
| 6 | **Stripe Connect + client payments** | Express connected-account onboarding + status mapping (`mapAccountStatus`), payment links (Checkout Session on the connected account + `PLATFORM_FEE_BPS` application fee), `Payment` ledger with `platformFeeCents`/`netCents`, refunds (UI + `charge.refunded` webhook), separate Connect webhook `POST /api/webhooks/stripe/connect` (own secret, own `WebhookEvent.provider`), fully isolated from SaaS billing. Client-subscription plumbing in place. ADR 0007, docs/deployment/stripe-connect.md | ✅ done |
| 7 | **Financeiro** | `/finance` dashboard — billed / received (net of refunds) / pending / refunds / avg ticket / platform fees / commissions KPIs from real rows (COMPLETED appointments + webhook-written `Payment`s, no fabricated figures); period presets (today/week/month/quarter/year/custom) in tenant tz; per-barber commissions (`commissionForAppointmentCents` — % or fixed, unit-tested); 6-month billed-vs-received bars; paginated payment log; CSV export route (`finance.read` gated) | ✅ done |
| 8 | **Notifications — e-mail + WhatsApp** | `src/features/messaging/`: system templates (pt-BR/en/es, `{{var}}`, per-tenant `MessageTemplate` override, HTML e-mail + text WhatsApp); `canContact` consent policy (WhatsApp/SMS opt-in required, e-mail transactional unless opted out); `dispatch` persists a `Message` row + attempt/`FAILED`+backoff `[1,5,30,120,360]`min / `retryDueMessages`; Resend e-mail (console transport when unconfigured) + **official WhatsApp Cloud API** (`graph.facebook.com/v21.0`), never simulated; `notifyAppointment`/`notifyPaymentLink` try WhatsApp→e-mail, stop at first `SENT`; async via BullMQ (`enqueueAppointmentNotification`) — never in the request path; wired on appointment create/reschedule/cancel + payment link; `cron:reminders` (`bookingConfig.reminderLeadHours`, `reminderSentAt` guard) + `cron:retry-messages` scheduled jobs; signed WhatsApp webhook `/api/webhooks/whatsapp` (GET verify + `x-hub-signature-256`, status receipts + inbound); `/messages` log (filters, status counts, pagination). ADR 0008, docs/deployment/{resend,whatsapp}.md | ✅ done |
| 9 | **Public booking page** | `/barber/{slug}` in 3 languages: service → barber → slot → payment → confirmation | ⬜ |
| 10 | **Chatbot IA** | Per-tenant, tool/function-calling grounded in backend data (never invents price/availability), language-aware, human handoff, Conversations panel | ⬜ |
| 11 | **Import · Campaigns · Loyalty/Reviews** | CSV/XLSX import (preview → dedupe → confirm), WhatsApp/e-mail campaigns with variables, feature-flagged loyalty & reviews | ⬜ |
| 12 | **Hardening** | Rate-limit coverage, audit-log coverage, cross-tenant isolation test suite, E2E on critical flows, empty/error states pass | ⬜ |

## Definition of done per slice

- Happy path + error/empty states implemented (no dead buttons)
- Input validated with zod; authorization checked on the server via `requireTenantContext`
- Every tenant-scoped query goes through `forTenant()`
- Automated test for the critical path (unit and/or E2E)
- No secret in client code; external integrations env-gated, never simulated
- `npm run typecheck && npm run lint && npm test` green
